# Realiza una función llamada catalogar() que reciba la lista de vehiculos y los recorra mostrando el nombre de su clase y sus atributos.

# Define a function named catalogar() that receives the list of vehicles and iterates over it, showing the name of its class and its attributes.
def catalogar(vehiculos):
    for vehiculo in vehiculos:
        print(vehiculo)

        